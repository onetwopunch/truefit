.bookmark
  .page-title
    %h1.text-center Artists

.pull-up{style: 'margin-top: -60px;'}
- @artists.each do |artist|
  .row
    .col-sm-10.col-xs-offset-2
      .artist-cover-photo
        %h2.artist-name= "#{artist.first_name} #{artist.last_name}"
        = link_to image_tag(artist.cover_photo), "artists/show/#{artist.id}"
        